interface:
  display_name: "Question to Prompt Pack"
  short_description: "Frame rough questions before prompting."
  default_prompt: "Use $question-to-prompt-pack to turn my rough question into a collaboration frame, then generate a final prompt."
policy:
  allow_implicit_invocation: true

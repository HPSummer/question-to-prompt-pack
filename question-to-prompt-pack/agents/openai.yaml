interface:
  display_name: "Question to Prompt Pack"
  short_description: "Frame rough questions, then route to the right skill."
  default_prompt: "Use $question-to-prompt-pack to understand my rough question, generate a concise prompt pack, then choose the best skill to execute it."
policy:
  allow_implicit_invocation: true
